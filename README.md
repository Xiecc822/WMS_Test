# Warehouse Management System (WMS)

## Quickstart

```bash
docker compose up --build
```

API available at `http://localhost:8000`. The Compose configuration lives at
`wms/docker-compose.yml` within this repository.

## Development

```bash
make dev
```

## Testing

```bash
make test
```
